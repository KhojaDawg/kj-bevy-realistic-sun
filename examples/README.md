## Examples

Run the following command with the example name

```bash
cargo run --example <example_name> --features "dev_features"
```

For example, to run the `minimal` example:

```bash
cargo run --example minimal --features "dev_features"
```

**Note:** the `dev_features` feature flag MUST be enabled in order for examples to run
